[project]
name = "geovlm-changeagent"
version = "0.1.0"
description = "Multimodal remote-sensing change interpretation agent"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "Pillow>=10.0", "gradio>=4.0"]

[tool.setuptools.packages.find]
where = ["src"]
